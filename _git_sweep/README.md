# _git_sweep

Preview and clean merged branches, stashes, build artifacts, and Git object storage.

```bash
bash _git_sweep/_git_sweep.sh --repo . --delete-merged-local --gc
bash _git_sweep/_git_sweep.sh --repo . --delete-merged-local --clean-artifacts --apply
```
